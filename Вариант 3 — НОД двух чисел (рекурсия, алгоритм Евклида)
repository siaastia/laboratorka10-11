def gcd(a, b):
    # Если второе число стало 0 — найден НОД
    if b == 0:
        return a
    # Иначе вызываем функцию от (b, a % b)
    return gcd(b, a % b)

# Пример
x = 48
y = 18
print("НОД =", gcd(x, y))
